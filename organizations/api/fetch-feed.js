import dns from 'node:dns/promises';
import net from 'node:net';
const BLOCKED_HOSTS=new Set(['localhost','localhost.localdomain','metadata.google.internal']);
function isPrivateIP(ip){if(net.isIP(ip)===4){const p=ip.split('.').map(Number);return p[0]===10||p[0]===127||p[0]===0||(p[0]===169&&p[1]===254)||(p[0]===172&&p[1]>=16&&p[1]<=31)||(p[0]===192&&p[1]===168)||(p[0]===100&&p[1]>=64&&p[1]<=127)||p[0]>=224}if(net.isIP(ip)===6){const x=ip.toLowerCase();return x==='::1'||x==='::'||x.startsWith('fc')||x.startsWith('fd')||x.startsWith('fe8')||x.startsWith('fe9')||x.startsWith('fea')||x.startsWith('feb')||x.startsWith('ff')}return true}
async function validateTarget(value){let url;try{url=new URL(value)}catch{throw new Error('Invalid URL')}if(!['http:','https:'].includes(url.protocol))throw new Error('Only HTTP and HTTPS are allowed');const host=url.hostname.toLowerCase();if(BLOCKED_HOSTS.has(host)||host.endsWith('.local'))throw new Error('Blocked hostname');const addresses=net.isIP(host)?[{address:host}]:await dns.lookup(host,{all:true,verbatim:true});if(!addresses.length||addresses.some(x=>isPrivateIP(x.address)))throw new Error('Private or unsafe target blocked');return url}
async function fetchSafe(initial){let current=initial;for(let redirects=0;redirects<=4;redirects++){await validateTarget(current.href);const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),12000);let response;try{response=await fetch(current,{redirect:'manual',signal:controller.signal,headers:{'user-agent':'Mahmoud-Salama-Source-Intelligence/1.0','accept':'application/rss+xml,application/atom+xml,application/json,text/csv,text/xml,application/xml,text/plain;q=0.8'}})}finally{clearTimeout(timer)}if([301,302,303,307,308].includes(response.status)){const location=response.headers.get('location');if(!location)throw new Error('Redirect without location');current=new URL(location,current);continue}return {response,current}}throw new Error('Too many redirects')}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const initial=await validateTarget(req.body?.url),{response,current}=await fetchSafe(initial);if(!response.ok)throw new Error(`Feed returned HTTP ${response.status}`);const type=response.headers.get('content-type')||'text/plain';if(!/xml|rss|atom|json|csv|text\/plain/i.test(type))throw new Error('Unsupported feed content type');const length=Number(response.headers.get('content-length')||0);if(length>2_000_000)throw new Error('Feed is larger than 2 MB');const content=await response.text();if(content.length>2_000_000)throw new Error('Feed is larger than 2 MB');res.setHeader('Cache-Control','no-store');return res.status(200).json({finalUrl:current.href,contentType:type,content,checkedAt:new Date().toISOString()})}catch(error){return res.status(400).json({error:error.name==='AbortError'?'Feed request timed out':error.message||'Feed fetch failed'})}}
