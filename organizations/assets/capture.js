import {escapeHTML,showToast,safeUrl} from './shell.js';
import {saveCustomRecord} from './data.js';
import {saveTracking,recordKey} from './tracker.js';
import {saveSnapshot} from './localdb.js';
import {detectATSFromUrl} from './productivity.js';
import {assessEligibility} from './eligibility.js';

const $=s=>document.querySelector(s);
function bookmarklet(origin){const target=`${origin}${location.pathname.replace(/[^/]+$/,'')}save-from-web.html`;return `javascript:(()=>{const s=(window.getSelection&&window.getSelection().toString()||'').slice(0,7000),d=document.querySelector('meta[name="description"]')?.content||'',c=document.querySelector('[data-company],.company-name,.job-company')?.textContent||'';window.open('${target}?capture=1&title='+encodeURIComponent(document.title)+'&url='+encodeURIComponent(location.href)+'&text='+encodeURIComponent(s||d)+'&company='+encodeURIComponent(c),'_blank','noopener')})()`}
function inferTitle(raw=''){return raw.replace(/\s+[|–—-]\s+.*$/,'').replace(/\bJobs? at\b.*$/i,'').trim().slice(0,300)}
function sourceName(url=''){try{return new URL(url).hostname.replace(/^www\./,'')}catch{return ''}}
export async function initCapture(){const p=new URLSearchParams(location.search),captured=p.get('capture')==='1',url=p.get('url')||'',detected=detectATSFromUrl(url),main=$('main');main.innerHTML=`<section class="dataset-hero"><div><span class="eyebrow">Browser capture tool</span><h1>Save to Career Hub</h1><p>Drag the bookmark to your bookmarks bar, then use it on any public vacancy page. The page title, URL and selected text are transferred to this local hub.</p></div><div class="hero-actions"><a class="button secondary" href="./ats-connectors.html">ATS Connectors</a><a class="button primary" href="./documents.html">Documents</a></div></section><section class="capture-layout"><article class="panel bookmarklet-panel"><h2>Install the bookmarklet</h2><p>Drag this button to the bookmarks bar. Some highly restricted sites block bookmarklets; the manual form remains available.</p><a class="button primary bookmarklet" id="bookmarkletLink" href="#">Save to Career Hub</a><ol><li>Open a job vacancy.</li><li>Select the most important description text, if useful.</li><li>Click <b>Save to Career Hub</b> in the bookmarks bar.</li><li>Review the fields and save locally.</li></ol><small>Target: ${escapeHTML(location.origin)}</small></article><article class="panel"><h2>${captured?'Review captured vacancy':'Add a vacancy manually'}</h2><form id="captureForm" class="tracking-form"><label class="full">Job title<input name="title" required value="${escapeHTML(inferTitle(p.get('title')||''))}"></label><label class="full">Organization<input name="subtitle" value="${escapeHTML(p.get('company')||sourceName(url))}"></label><label class="full">Source URL<input type="url" name="url" required value="${escapeHTML(url)}"></label><label>Country / location<input name="country" value="${escapeHTML(p.get('location')||'')}"></label><label>Source channel<input name="sourceChannel" value="${escapeHTML(sourceName(url))}"></label><label>Contract type<input name="contractType"></label><label>Deadline<input type="date" name="deadline"></label><label class="full">Job description / selected text<textarea name="notes">${escapeHTML(p.get('text')||'')}</textarea></label><div class="capture-detection full"><span>Detected platform</span><b id="captureATS">${escapeHTML(detected.label)}</b></div><div class="form-actions full"><button class="button primary" type="submit">Save vacancy locally</button></div></form></article></section><section class="panel"><h2>Privacy and security</h2><p>The bookmarklet transfers only the current page title, URL, selected text or meta description to your Career Hub page. It does not submit applications, collect passwords or access browser history.</p></section>`;const link=$('#bookmarkletLink');link.href=bookmarklet(location.origin);link.onclick=e=>{if(e.isTrusted){e.preventDefault();showToast('Drag this button to the bookmarks bar instead of clicking it.')}};$('#captureForm').onsubmit=async e=>{e.preventDefault();const v=Object.fromEntries(new FormData(e.target)),id=`web-${Date.now()}`,eligibility=assessEligibility(v),record=saveCustomRecord({id,dataset:'jobs',title:v.title,subtitle:v.subtitle,type:'Captured named vacancy',region:'',country:v.country,location:v.country,fit:'High',status:'Open',posted:'',checked:new Date().toISOString().slice(0,10),notes:v.notes,source:v.sourceChannel||'Saved from web',url:v.url,atsProvider:detected.provider,atsIdentifier:detected.identifier,contractType:v.contractType,remoteEligibility:eligibility.label});saveTracking({...record,recordType:'job',availability:'Open',trust:'Personal capture',trustScore:3,eligibility},{status:'Saved',deadline:v.deadline,sourceChannel:v.sourceChannel,contractType:v.contractType,remoteEligibility:eligibility.label,atsProvider:detected.provider},{action:'Saved from browser bookmarklet',type:'capture'});if(v.notes)await saveSnapshot(recordKey(record),{title:v.title,content:v.notes,sourceUrl:v.url,captureMethod:'bookmarklet'});showToast('Vacancy saved to All Jobs and My Applications');e.target.reset();setTimeout(()=>location.href='./tracker.html',500)}}
