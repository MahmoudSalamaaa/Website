import {renderShell,registerServiceWorker,showToast} from './shell.js';
import {loadCareerDecisionData,analyzeJob,generateApplicationPack,trackerMetrics,saveAnalysisToTracker} from './career-decision-engine.js';
const $=s=>document.querySelector(s);let data,result,pack,activePack='cover';
renderShell();registerServiceWorker();boot().catch(e=>{$('#ccMain').innerHTML=`<section class="cc-panel"><h1>Unable to open Career Command Center</h1><p>${escapeHTML(e.message||e)}</p></section>`});
function escapeHTML(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function boot(){data=await loadCareerDecisionData();renderBase();renderMetrics();bind()}
function renderBase(){const main=$('#ccMain');main.innerHTML=`<section class="cc-hero"><div><span class="cc-badge">Career Operating System 2.0</span><h1>From vacancy to evidence-backed application.</h1><p>Analyze the job, identify eligibility and evidence gaps, route it to the strongest frozen CV, prepare application material, save it to the tracker and follow the outcome.</p></div><div class="cc-hero-side"><a href="./ats-connectors.html"><strong>Official ATS</strong><span>Greenhouse · Lever · SmartRecruiters</span></a><a href="./analytics.html"><strong>Application Analytics</strong><span>CV · source · country conversion</span></a><a href="./tracker.html"><strong>Tracker</strong><span>Applications · interviews · offers</span></a><a href="../waca/"><strong>Interview Lab</strong><span>Canonical WACA preparation workspace</span></a></div></section><div id="ccMetrics"></div><div class="cc-grid"><div><section class="cc-panel"><h2>1. Job Decision Engine</h2><p class="muted">Paste the official job description. The score is constrained by the verified evidence bank.</p><form id="jobForm" class="cc-form"><label>Job title<input name="title" placeholder="IT Director / Enterprise Architect"></label><label>Company / organization<input name="company" placeholder="Organization name"></label><label>Location<input name="location" placeholder="Muscat, Oman / Dubai / Cairo / Remote"></label><label>Official vacancy URL<input name="url" type="url" placeholder="https://..."></label><label class="full">Job description<textarea name="description" required placeholder="Paste the full job description here..."></textarea></label><div class="cc-actions"><button class="cc-primary" type="submit">Analyze opportunity</button><button class="cc-secondary" type="button" id="sampleBtn">Load sample structure</button><button class="cc-secondary" type="reset">Clear</button></div></form></section><section class="cc-panel" id="analysisPanel"><div class="cc-empty">Run an analysis to see the decision, CV route and evidence.</div></section><section class="cc-panel" id="packPanel"><div class="cc-empty">The application pack will appear after analysis.</div></section></div><aside><section class="cc-panel" id="evidencePanel"><h2>Evidence Graph</h2><p class="muted">Verified achievements are matched to requirements. No invented evidence is allowed.</p><div class="cc-empty">Evidence matches will appear here.</div></section><section class="cc-panel" id="followPanel"><h2>Follow-up Queue</h2><div class="cc-empty">No tracker data loaded yet.</div></section><section class="cc-panel"><h2>Public recruiter assets</h2><div class="queue"><article><strong>60-second recruiter view</strong><span>Short executive narrative + frozen CV selection.</span><a href="../recruiter/" target="_blank">Open recruiter page →</a></article><article><strong>Evidence case studies</strong><span>Problem → mandate → approach → result.</span><a href="../case-studies/" target="_blank">Open case studies →</a></article><article><strong>Executive portfolio</strong><span>Full leadership portfolio and proof of work.</span><a href="../executive-portfolio/" target="_blank">Open portfolio →</a></article></div></section></aside></div>`}
function bind(){$('#jobForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget),input=Object.fromEntries(f.entries());result=analyzeJob(input,data);pack=generateApplicationPack(result,data);renderAnalysis();renderEvidence();renderPack();renderMetrics()};$('#sampleBtn').onclick=()=>{const f=$('#jobForm');f.title.value='Technology / Digital Transformation Director';f.company.value='Target Organization';f.location.value='Muscat, Oman';f.description.value='Lead digital transformation, enterprise architecture, systems integration, data platforms, program governance, vendor management, software delivery and stakeholder engagement across a public-sector technology portfolio.'}}
function renderMetrics(){const m=trackerMetrics();$('#ccMetrics').innerHTML=`<section class="cc-panel"><div class="metric-grid-cc"><article><strong>${m.tracked}</strong><span>Tracked opportunities</span></article><article><strong>${m.submitted}</strong><span>Applications</span></article><article><strong>${m.responseRate}%</strong><span>Response rate</span></article><article><strong>${m.interviewRate}%</strong><span>Interview rate</span></article><article><strong>${m.offers}</strong><span>Offers</span></article></div>${m.bestCv?`<p class="muted">Best recorded CV conversion so far: <b>${escapeHTML(m.bestCv.name)}</b> · ${m.bestCv.interviews}/${m.bestCv.apps} interview(s).</p>`:''}</section>`;renderFollowups(m)}
function renderFollowups(m){const box=$('#followPanel');if(!box)return;const due=m.due||[];box.innerHTML=`<h2>Follow-up Queue</h2><p class="muted">Items due within seven days based on the existing tracker.</p>${due.length?`<div class="queue">${due.slice(0,8).map(x=>`<article><strong>${escapeHTML(x.recordSnapshot?.title||'Tracked opportunity')}</strong><span>${escapeHTML(x.recordSnapshot?.subtitle||'')}</span><small>${escapeHTML(x.followUp||x.nextContactDate||'Due')}</small></article>`).join('')}</div>`:'<div class="cc-empty">No follow-ups due in the next seven days.</div>'}<p><a href="./today.html">Open full Today queue →</a></p>`}
function renderAnalysis(){const r=result,parts=r.parts;$('#analysisPanel').innerHTML=`<h2>2. Decision</h2><div class="cc-score"><div class="cc-score-ring" style="--score:${r.score}"><strong>${r.score}%</strong></div><div><span class="cc-decision ${r.decision.key}">${escapeHTML(r.decision.label)}</span><h3>${escapeHTML(r.route.label)}</h3><p class="muted">Recommended frozen CV family${r.route.fallback?' · fallback route':''}.</p><div class="cv-card"><strong>${escapeHTML(r.route.pdf)}</strong><a href="../docs/${encodeURIComponent(r.route.pdf)}" target="_blank">Open CV →</a></div></div></div><div class="cc-breakdown">${Object.entries(parts).map(([k,v])=>`<div><strong>${v}%</strong><span>${escapeHTML(k.replaceAll('_',' '))}</span></div>`).join('')}</div>${r.eligibility.hard.length?`<div class="risk-list"><div class="risk hard"><b>Eligibility blocker:</b> ${escapeHTML(r.eligibility.hard.join(', '))}</div></div>`:''}${r.gaps.length?`<h3>Validate before applying</h3><div class="risk-list">${r.gaps.map(x=>`<div class="risk">${escapeHTML(x)}</div>`).join('')}</div>`:'<p class="muted">No material rule-based gap detected. Still verify every mandatory requirement in the official vacancy.</p>'}<div class="cc-actions"><button class="cc-primary" id="saveAnalysis">Save to tracker</button><a class="cc-secondary" href="../docs/${encodeURIComponent(r.route.pdf)}" target="_blank">Open recommended CV</a></div>`;$('#saveAnalysis').onclick=()=>{const key=saveAnalysisToTracker(result,pack);showToast(`Saved to tracker: ${key}`);renderMetrics()}}
function renderEvidence(){const list=result.coverage.achievements;$('#evidencePanel').innerHTML=`<h2>Evidence Graph</h2><p class="muted">${result.coverage.requested.length} relevant capability area(s) detected in the job description.</p>${list.length?`<div class="evidence-list">${list.map(e=>`<article class="evidence-card"><h3>${escapeHTML(e.title)}</h3><p>${escapeHTML(e.summary)}</p><div class="metric-chips">${(e.metrics||[]).map(m=>`<span>${escapeHTML(m)}</span>`).join('')}</div><p><a href="..${escapeHTML(e.source)}" target="_blank">Source evidence →</a></p></article>`).join('')}</div>`:'<div class="cc-empty">No mapped evidence was triggered. This is a signal to review the role manually rather than inventing a fit.</div>'}`}
function packText(){if(!pack)return '';if(activePack==='cover')return pack.cover;if(activePack==='email')return pack.email;if(activePack==='linkedin')return pack.linkedin;if(activePack==='screening')return pack.screening.map((x,i)=>`${i+1}. ${x.q}\n${x.a}`).join('\n\n');return `Interview focus:\n- ${pack.interviewFocus.join('\n- ')}\n\nQuestions to ask:\n- ${pack.questions.join('\n- ')}`}
function renderPack(){const tabs=[['cover','Cover letter'],['email','Recruiter email'],['linkedin','LinkedIn'],['screening','Screening answers'],['interview','Interview brief']];$('#packPanel').innerHTML=`<h2>3. Evidence-safe Application Pack</h2><p class="muted">Drafts use only the verified evidence bank plus the job fields you entered.</p><div class="pack-tabs">${tabs.map(([k,l])=>`<button type="button" data-pack="${k}" class="${activePack===k?'active':''}">${l}</button>`).join('')}</div><div class="pack-output" id="packOutput">${escapeHTML(packText())}</div><div class="cc-actions"><button class="cc-secondary" id="copyPack">Copy current draft</button></div>`;document.querySelectorAll('[data-pack]').forEach(b=>b.onclick=()=>{activePack=b.dataset.pack;renderPack()});$('#copyPack').onclick=async()=>{await navigator.clipboard.writeText(packText());showToast('Draft copied')}}
