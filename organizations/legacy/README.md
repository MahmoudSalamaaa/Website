# Legacy snapshot

`regional-hub-original-upload.html` is the exact HTML file uploaded in the conversation before this unified rebuild. It is retained only as a reference snapshot; the active application uses the pages in the parent folder.
